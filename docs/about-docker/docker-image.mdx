---
sidebar_position: 3
---

# Docker Images

컨테이너를 여러 번 만들다 보면 한 가지 문제가 발생한다. 컨테이너를 삭제해도 이미지는 그대로 남아 쌓이기 때문이다.

```shell
docker image rm 이미지_이름
```

때문에 image는 따로 명령어를 입력해 지워주어야 한다. 해당 이미지로 실행한 컨테이너가 남아있다면, 이미지를 삭제할 수 없다.

```shell
docker image ls
```
